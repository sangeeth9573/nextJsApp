/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['zps36re6j3v99dcn.public.blob.vercel-storage.com'], // Add the domains you want to allow
      },
};

export default nextConfig;
